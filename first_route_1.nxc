/* TOTAL: 66 pts */

#include "godzilla_api.h"

/* Primary position on the Base
   Alinhamento: canto esquerdo
*/

task main(){

    // Go to Pets
    move_arm(180, MEDIUM_POWER, UP);
    move(76, HIGH_POWER, FORWARD);
    move_arm(180, MEDIUM_POWER, DOWN);
    
    // Go to Base
    move(76, HIGH_POWER , BACKWARD);
}
